services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3001:3000"
    volumes:
      - /docker_react/node_modules
      - .:/docker_react

  tests:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - /docker_react/node_modules
      - .:/docker_react
    command: npm run test -- --watchAll
    stdin_open: true
    tty: true
    environment:
      CI: "false"
